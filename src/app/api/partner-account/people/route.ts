import { NextResponse } from 'next/server'; import { z } from 'zod'; import { requireUser } from '@/lib/auth'; import { createSupabaseAdminClient } from '@/lib/supabase/admin';
const input=z.object({name:z.string().trim().min(2).max(200),email:z.string().trim().email(),role:z.enum(['decision_maker','budget_owner','organization_admin','leader','coach','implementation_contact']),organizationName:z.string().trim().max(200).optional(),notes:z.string().trim().max(500).optional()});
export async function POST(request:Request){try{const user=await requireUser();const p=input.parse(await request.json());const admin=createSupabaseAdminClient();const app=await admin.from('partner_applications').select('id,status').eq('auth_user_id',user.id).single();if(app.error||!app.data)return NextResponse.json({error:'Partner application not found.'},{status:404});if(['rejected','suspended'].includes(app.data.status))return NextResponse.json({error:'This partner account cannot add people.'},{status:403});const row=await admin.from('affiliate_people').insert({partner_application_id:app.data.id,name:p.name,email:p.email.toLowerCase(),role:p.role,organization_name:p.organizationName||null,notes:p.notes||null}).select('id,name,email,role,organization_name,invitation_status').single();if(row.error)return NextResponse.json({error:row.error.code==='23505'?'This person already exists for that role.':'Unable to add person.'},{status:400});return NextResponse.json({person:row.data});}catch{return NextResponse.json({error:'Unable to add affiliate contact.'},{status:400});}}
