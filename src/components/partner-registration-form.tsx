'use client';
import { useState } from 'react';
export function PartnerRegistrationForm() { const [form,setForm]=useState({organizationName:'',contactName:'',email:'',password:'',phone:'',website:'',address:''}); const [message,setMessage]=useState(''); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');setMessage('');try{const res=await fetch('/api/partner-account/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await res.json();if(!res.ok)throw new Error(data.error);setMessage(data.message);setForm({...form,password:''});}catch(err){setError(err instanceof Error?err.message:'Unable to create partner account.');}finally{setBusy(false);}} return <form onSubmit={submit} className="mt-8 grid gap-4 sm:grid-cols-2">{([['organizationName','Partner organization','text'],['contactName','Contact name','text'],['email','Email','email'],['password','Password (8+ characters)','password'],['phone','Phone','tel'],['website','Website','url']] as const).map(([key,label,type])=><label key={key} className="text-sm font-semibold text-slate-700">{label}<input required={['organizationName','contactName','email','password'].includes(key)} type={type} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 font-normal"/></label>)}<label className="text-sm font-semibold text-slate-700 sm:col-span-2">Business address<textarea value={form.address} onChange={e=>setForm({...form,address:e.target.value})} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 font-normal" rows={3}/></label><button disabled={busy} className="rounded-full bg-blue-700 px-5 py-3 font-semibold text-white sm:col-span-2">{busy?'Creating account…':'Create partner account'}</button>{error&&<p className="text-red-700 sm:col-span-2">{error}</p>}{message&&<p className="rounded-xl bg-teal-50 p-4 text-teal-900 sm:col-span-2">{message}</p>}</form>; }
