const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const ts=require('typescript');
test('browser auth leaves one-use URL codes for the explicit confirmation handler',()=>{let options;const module={exports:{}};const code=ts.transpileModule(fs.readFileSync('src/lib/supabase/browser.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText;new Function('require','module','exports',code)(name=>name==='@supabase/ssr'?{createBrowserClient:(url,key,opts)=>{options=opts;return{};}}:{getClientEnv:()=>({NEXT_PUBLIC_SUPABASE_URL:'https://example.supabase.co',NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY:'test'})},module,module.exports);module.exports.createSupabaseBrowserClient();assert.equal(options.auth.detectSessionInUrl,false);assert.equal(options.auth.flowType,undefined);assert.equal(options.cookies,undefined);});
