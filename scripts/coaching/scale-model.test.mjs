import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {createRequire} from 'node:module';const require=createRequire(import.meta.url),ts=require('typescript');const code=ts.transpileModule(fs.readFileSync(new URL('../../src/lib/coaching/scale.ts',import.meta.url),'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText;const module={exports:{}};new Function('module','exports',code)(module,module.exports);const {supplyStatus,metricLabel,analyticsFilters}=module.exports;
test('missing observations are distinct from zero',()=>{assert.equal(metricLabel(null),'Not available');assert.equal(metricLabel(0),'0');});
test('supply indicators preserve unknown capacity and denominator',()=>{assert.equal(supplyStatus(null,10),'Capacity incomplete');assert.equal(supplyStatus(0,0),'No open demand');assert.equal(supplyStatus(2,9),'Critical shortage');assert.equal(supplyStatus(8,10),'Shortage');assert.equal(supplyStatus(15,10),'Watch');assert.equal(supplyStatus(20,10),'Healthy');});
test('analytics filters exclude arbitrary fields and confidential search text',()=>{assert.deepEqual(analyticsFilters({from:'2026-01-01',organization:'one',notes:'private',q:'person'}),{from:'2026-01-01',organization:'one'});});
